import { makeStyles } from '@mui/styles'

const Styles = makeStyles(theme => ({
  containerPage: {
    position: 'relative',
    zIndex: 0
  },
  contentPage: {
    position: 'relative',
    zIndex: 2
  }
}))

export default Styles
